-- Действующий механический закон съезда. Одно значение = одно поколение:
-- включение нового закона перезаписывает прежний (тот уходит в архив).
-- 'none' — ничего не действует. 'teleport' — команды могут телепортироваться
-- при захвате (см. submission.service): цель — сектор той же сложности, что
-- якорь команды, цена 75 опыта, без лимита.
INSERT INTO game_settings (key, value)
VALUES ('active_law', 'none')
ON CONFLICT (key) DO NOTHING;
