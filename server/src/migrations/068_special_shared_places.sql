-- Особые события теперь допускают ничью: несколько команд могут занять одно
-- место (например, две команды делят 2-е). Каждая такая команда получает
-- полный бандл награды этого места. Снимаем уникальность места в рамках
-- события; ограничение «одна строка на команду» остаётся.
ALTER TABLE special_sector_awards
  DROP CONSTRAINT IF EXISTS special_sector_awards_sector_id_place_key;
