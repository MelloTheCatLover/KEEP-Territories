# features/auth

Authentication — login, register, current user, route guards.

## Files
- `types.ts` — User, AuthResponse, payloads
- `api.ts` — loginRequest, registerRequest, meRequest (thin wrappers over apiClient)
- `LoginPage.tsx` — placeholder, real UI in M13
